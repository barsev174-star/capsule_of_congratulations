# Gift intro legacy v1

This folder keeps the complete gift-opening implementation that preceded the
assembled-card experiment. It remains executable for fallback and visual
comparison; avoid folding experimental changes into this snapshot.

- Current implementation: default route behavior.
- Current implementation with full motion forced: `?motion=full`.
- Legacy implementation: `?intro=legacy`.
