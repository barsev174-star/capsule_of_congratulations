# Универсальная семья шаблонов `universal-v1`

Статус: утверждённый контракт этапа 1.

Дата: 11 августа 2026 года.

Связанные документы:

- `TEMPLATE_AUTOMATION_IMPLEMENTATION_PLAN_2026-08-11.md`;
- `TEMPLATE_CREATION_RUNBOOK.md` — рабочая инструкция, asset- и acceptance-чек-листы;
- `LEGACY_TEMPLATE_BASELINE_2026-08-11.md`;
- задача `P2-13` в сводном списке замечаний.

## 1. Граница семьи

`universal-v1` — не произвольный конструктор страниц. Все шаблоны этой семьи используют одинаковые семантические блоки, правила контента, адаптивные состояния и экспортные композиции. Между оформлениями меняются художественные ассеты, разрешённые цвета, шрифты и ограниченные токены геометрии.

В профиле запрещены:

- произвольные React-компоненты;
- HTML-фрагменты;
- CSS-строки и селекторы;
- исполняемые callbacks;
- отдельные ветки по `templateId` в private, public и export-renderer.

Если новое оформление не помещается в этот контракт, для него проектируется новая структурная семья.

## 2. Утверждённые решения

| Механика | Что показал аудит Paper/Route | Рекомендуемый стандарт `universal-v1` | Статус |
|---|---|---|---|
| Порядок блоков | Web поддерживает пользовательскую перестановку, export имеет фиксированную композицию | Фиксированный порядок внутри семьи; можно включать и выключать только необязательные блоки | Утверждено |
| Состав private | Оба legacy-шаблона используют семь одинаковых семантических блоков | `hero → summary → qualities → messages → memories → quotes → closing` | Утверждено |
| Обязательные блоки | В Paper/Route обязательны `hero`, `summary`, `messages`, `closing` | Сохранить этот набор обязательным | Утверждено |
| Необязательные блоки | `qualities`, `memories`, `quotes` зависят от контента и настроек | Разрешить включение/выключение; пустой блок не рендерить | Утверждено |
| Public | Сейчас показывает только hero, qualities, разрешённые фото и quotes | `hero → qualities? → memories? → quotes? → public-note`; без summary, личных поздравлений и private footer | Утверждено |
| Шесть схем поздравлений | Реально работают три схемы без фото и три с 1/2/3 фото | Сохранить все шесть как общий контракт семьи | Утверждено |
| Карточка поздравления | Paper показывает аватар/инициалы, Route — компактного автора без аватара | Общий каркас: автор, необязательная роль, текст; аватар — необязательный визуальный слот профиля | Утверждено |
| Кадрирование | Карточка хранит `cropX/cropY` в процентах `0…100` и `cropZoom` | Legacy-хранение не менять; в universal view/public/export нормализовать координаты в `0…1` одним адаптером | Утверждено |
| Фоторамка | Paper и Route используют base/overlay и разные окна/подписи | Единый контракт `base? + aperture + overlay? + captionArea + fit`; вся геометрия `0…1` | Утверждено |
| Подпись фото | Текущий редактор ограничивает подпись 45 символами | 45 символов — жёсткий входной лимит и одновременно гарантированный полный вывод в любой рамке; многоточие запрещено | Утверждено |
| Качества | В продукте и export используются пять качеств | Ровно 5 значений, до 28 символов каждое, максимум две строки; в Story/Post/A4 сетка `2–1–2`: средняя карточка центрируется, пары разделены интервалом шириной в одну карточку; профиль может подключить отдельные пять горизонтальных `exportQualityCards`, не меняющие Web | Уточнено по экспортам 16.08.2026 |
| Лучшие фразы | Существующий поток уже различает кандидатов, рекомендации организатора и выбор получателя | Найти 3–6 кандидатов; организатор рекомендует ровно 3; получатель позже выбирает ровно 3 из полного набора; поверхность показывает 2–3 выбранные фразы | Утверждено |
| «Моменты» | Обновлённые Story/Post/Print «Маршрута» используют акцентный коллаж | Блок использует собственные 3 фото и не зависит от числа фото в поздравлениях; Story/A4 — крупное фото слева с заголовком над ним и две малые фотографии столбцом справа; Post — крупное фото слева и одна малая справа с заголовком над ней; все карточки слегка повёрнуты | Уточнено по экспортам 14.08.2026 |
| Hero | Есть повод, имя, описание и счётчики; дата события уже хранится в «Основе открытки» | Под поводом показывать дату вида `01 января 2026`; при отсутствии не выводить; public/export управляются отдельным `showEventDate` | Утверждено |
| Главное о тебе | Полный текст больше негде посмотреть | Жёсткий лимит 500 символов; все 500 видны без многоточия, после текста обязательно выводится имя автора | Уточнено по Route 11.08.2026 |
| Анимация открытия | Использует облегчённую карточку, а не полный `FinalCard` | Миниатюра строится только из токенов профиля: accent, surface, text, mark/pattern; тяжёлые ассеты не загружаются | Утверждено |
| Декоративные слои | Paper допускает anchored images и текстовые notes; Route в основном использует готовые поверхности | Поддержать section surface, component base/overlay и anchored image; произвольный декоративный текст запретить | Утверждено |
| Footer | Private, public и export используют разный смысловой состав | Три явных адаптера контента используют единую профильную поверхность `sections.closing`: private signature/actions, public privacy note/actions/brand, export privacy note/brand | Утверждено |
| Story/Post/A4 | Это отдельные фиксированные композиции Satori | Сохранить отдельные адаптеры с общими данными, ассетами, рамками и safe areas; не пытаться исполнять browser CSS | Утверждено |
| A4 | Текущий export — `1240 × 1754`, примерно 150 DPI | Зафиксировать 150 DPI для v1; 300 DPI — отдельное решение и бюджет | Утверждено |

Визуальные примеры текущих механик находятся в `screenshots/template-legacy-baseline/`: восемь web-состояний и Story/Post/Print для Paper и Route. Для экспортов канонический эталон — обновлённый Route от 14.08.2026: акцентное крупное фото слева и вертикальная группа справа. Web-композиции остаются адаптивными и не копируют fixed-canvas геометрию буквально.

## 3. Канонический порядок и доступность блоков

| Блок | Private | Public | Story/Post/A4 | Обязательность |
|---|---|---|---|---|
| `hero` | Да | Да | Да | Обязательный |
| `summary` | Да | Нет | Нет | Обязательный private-блок |
| `qualities` | Да | При разрешении | Да | Необязательный |
| `messages` | Да | Нет | Нет | Обязательный private-блок |
| `memories` | При ровно 3 фото | При ровно 3 разрешённых фото | Источник — ровно 3 разрешённых фото; Story/A4 показывают 3, Post — первые 2 | Необязательный |
| `quotes` | 2–3 из рекомендаций организатора | 2–3 из выбранных получателем | 2 для Story, 3 для Post/A4 | Необязательный |
| `closing` | Да | Нет | Нет | Обязательный private-блок |
| `public-note` | Нет | Да | Да | Системный контент на профильной поверхности `sections.closing` |

Рекомендуется не переносить пользовательский `finalBlockOrder` в `universal-v1`. Поле остаётся рабочим для legacy-шаблонов, но новая семья использует канонический порядок. Это исключает расхождение web/export и не превращает профиль в конструктор страниц.

## 4. Шесть схем поздравлений

| ID сценария | Композиция | Поздравлений в основном состоянии | Бюджет одного текста | Фото |
|---|---|---:|---:|---:|
| `grid-2` | Сетка 2 × 2 | 4 | 280 символов | 0 |
| `carousel-1` | Один ряд | 3 | 340 символов | 0 |
| `carousel-2` | Два ряда по 3 | 6 | 200 символов | 0 |
| `portrait` | Колонка поздравлений + вертикальное фото | 3 | 280 символов | 1 |
| `landscape-pair` | Колонка поздравлений + два горизонтальных фото | 4 | 280 символов | 2 |
| `landscape-trio` | Прокручиваемая колонка поздравлений + три горизонтальных фото, распределённых по высоте и смещённых по горизонтали | 4 | 280 символов | 3 |

Для `carousel-2` предлагается зафиксировать фактический лимит renderer — 200 символов — и исправить подпись редактора на этапе внедрения новой семьи. Legacy-поведение и сохранённые открытки при этом не меняются.

На desktop при наличии фото все поздравления пролистываются внутри ограниченной по высоте колонки. Для одного вертикального фото одновременно видны три карточки, для двух или трёх горизонтальных фото — четыре. Пара центрируется по высоте как единая группа; три фото распределяются по высоте и имеют Route-смещения по горизонтали. Схемы без фото сохраняют исходную геометрию: `grid-2` — 2 × 2, `carousel-1` — один ряд из трёх столбиков, `carousel-2` — два ряда по три столбика. Дополнительные поздравления доступны через штатную горизонтальную полосу прокрутки; обязательный scroll snap фиксирует смещение на соседнем столбике. Отдельных стрелок поверх открытки нет. Кнопки «Все поздравления» под блоком на desktop также нет. Счётчик с подписью `12 поздравлений` открывает диалог со всей подборкой.

На mobile порядок фиксирован и не зависит от выбранной desktop-схемы: заголовок и кликабельный счётчик → 0–3 выбранных фото → ровно 4 поздравления → кнопка `Посмотреть все N поздравлений`. Верхний счётчик и нижняя кнопка открывают один общий диалог. После закрытия восстанавливаются позиция прокрутки и фокус того элемента, из которого диалог был открыт.

Hero во всех шаблонах показывает число поздравлений и число фотографий. В private число фотографий равно сумме реально показанных фото в поздравлениях и «Моментах»; в public используется только разрешённый агрегат `publicPhotoCount`. Story, Post и A4 получают тот же публичный агрегат, поэтому скачанный файл совпадает с публичной страницей. Числа используют табличные цифры.

Секционные художественные подложки не растягиваются на переменную мобильную высоту. Для `adaptive-frame` mobile использует заполненный nine-slice-слой: углы сохраняются, верх и низ подгоняются по ширине, боковые орнаменты повторяются по высоте, а центр берётся из того же художественного изображения. Для `cover` с композиционно важным декором профиль подключает отдельный `mobileAsset`; он сохраняет пропорции и не обрезает элементы на границах. Desktop и export используют основной профильный ассет. Публичный `public-note` сохраняет собственный системный текст, но визуально использует ту же подключаемую `sections.closing`, что и приватный подвал.

Эти правила не копируются в каждый новый шаблон. Они входят в структурный preset `route-v1` из `src/lib/templates/layout-presets.ts`; `TemplateProfile` выбирает preset, а художественный профиль задаёт только ассеты, цвета, типографику и нормализованную геометрию рамок. Количество выбранных фото автоматически определяет media-сценарий, поэтому несовместимые комбинации вроде одного фото со схемой trio не допускаются.

## 5. Контентные лимиты

| Поле | Лимит | Поведение при переполнении |
|---|---:|---|
| Имя получателя | 46 символов | До двух строк и ступенчатое уменьшение шрифта; многоточие допустимо только для аномальных имён у верхней границы 41–46 символов |
| Повод | 60 символов | Одна-две строки; затем многоточие |
| Дата события | ISO `YYYY-MM-DD` в данных | Формат вывода `dd MMMM yyyy` с русским месяцем в родительном падеже; под поводом; пустое значение не создаёт строку или отступ |
| Hero description | Системный текст | Не задаётся шаблоном |
| Главное о тебе | 500 символов | Жёсткий лимит; абзацы, без прокрутки и без многоточия; после текста выводится автор |
| Поздравление участника | 200/280/340 по схеме | Обрезка только в карточке; полный текст доступен в диалоге |
| Качество | 28 символов | До двух строк; далее отклонение fixture/profile |
| Лучшая фраза | 100 символов | До трёх строк; далее отклонение |
| Заголовок «Моментов» | 80 символов | До двух строк; далее отклонение |
| Описание «Моментов» | 180 символов | До трёх строк; далее многоточие |
| Подпись фото | 45 символов | Полный вывод без многоточия; до двух строк и shrink-to-fit в пределах утверждённого минимального кегля; рамка, не вмещающая тестовую подпись из 45 символов, не проходит валидацию |
| Private signature | 180 символов | До трёх строк; далее многоточие |

Лимиты считаются частью семейного контракта и одинаково валидируются для web, public и export до рендера.

## 6. Контракт фоторамки и кадрирования

```ts
type NormalizedRect = {
  x: number;      // 0…1
  y: number;      // 0…1
  width: number;  // 0…1
  height: number; // 0…1
};

type UniversalPhotoFrame = {
  preset: UniversalPhotoFramePresetId;
  base?: AssetRef;
  overlay?: AssetRef;
  fit: "cover";
  caption: {
    maxChars: 45;
    maxLines: 2;
    align: "left" | "center" | "right";
    fontToken: "body" | "handwritten";
    fontStyle?: "normal" | "italic";
    fontScale?: number; // 0.75…1.5
    minScale: number;
    layout?: "standard" | "expanded";
  };
};

type UniversalPhotoCrop = {
  x: number;    // 0…1
  y: number;    // 0…1
  zoom: number; // 1…разрешённый максимум
};
```

Один и тот же `UniversalPhotoCrop` передаётся в private, public и export. Преобразование текущих процентов `0…100` выполняется в одном legacy-адаптере и не дублируется по поверхностям.

`aperture`, `captionArea`, исходный размер ассета и `aspectRatio` принадлежат общему preset-у и не дублируются в профиле. `fontScale` меняет базовый кегль подписи, а `minScale` ограничивает shrink-to-fit у предельного текста. Оба коэффициента одинаково исполняются Web- и Export-renderer-ами; подпись длиной 45 символов должна полностью помещаться в две строки без многоточия.

Для `memories` профиль не задаёт произвольное количество карточек. Каноническое состояние содержит ровно три фото:

- mobile web до `640px`: три полноширинные карточки в одном столбце;
- desktop web использует собственную адаптивную композицию семейства;
- Story и A4: крупная первая карточка слева, над ней название и описание блока; вторая и третья карточки меньше и стоят столбцом справа;
- Post: крупная первая карточка слева, вторая карточка справа, название и описание блока находятся над правой карточкой; третья фотография в компактный формат не входит;
- во всех export-форматах карточки имеют небольшие разные повороты, при этом фото, рамка, overlay и подпись поворачиваются единым контейнером.

Если в private или public разрешено не ровно три фото, блок целиком не рендерится. Интерфейс выбора для новой семьи предлагает состояния «не публиковать» или «выбрано 3 из 3», без промежуточной композиции для одного или двух фото.

## 7. Дата события и публичная видимость

Источник даты — `card.eventDate`, уже редактируемый в «Основе открытки → Дополнительные настройки». В семантической модели Hero дата остаётся ISO-строкой, а форматируется общим formatter непосредственно перед рендером.

Правила одинаковы для private, public и export:

- дата располагается сразу под поводом и до имени получателя;
- формат: `01 января 2026`;
- при `null` или пустом значении дата и её вертикальный отступ отсутствуют;
- public-настройка получает независимый флаг `showEventDate`, расположенный рядом с `showOccasion`, `showGreetingCount` и `showPhotoCount`;
- при наличии даты переключатель по умолчанию включён; если даты нет, переключатель не показывается;
- Story/Post/A4 используют тот же `showEventDate`, поэтому preview и скачиваемый файл совпадают с публичной страницей.

## 8. Поток «Лучших фраз»

1. Анализ поздравлений создаёт от 3 до 6 уникальных кандидатов, каждый не длиннее 100 символов.
2. Организатор отмечает ровно 3 рекомендации. Остальные кандидаты не удаляются.
3. Получатель при настройке публичной версии видит весь набор из 3–6 кандидатов, рекомендации организатора отмечены, и выбирает ровно 3 фразы в нужном порядке.
4. Private-открытка использует рекомендации организатора, public и публичные exports — выбор получателя.
5. Web, Post и A4 показывают 3 фразы; Story показывает первые 2 из выбранной тройки. Ни одна поверхность не показывает невыбранного кандидата.

## 9. Разрешённые слои и токены профиля

Профиль может задавать:

- `SectionSurface`: один фон или готовая поверхность блока;
- `ComponentFrame`: необязательный base, окно контента, необязательный overlay;
- `AnchoredImage`: декоративное изображение, привязанное к семантическому anchor;
- цвета `page`, `text`, `muted`, `accent`, `surface`;
- шрифты `heading`, `body`, `handwritten` из разрешённого реестра;
- ограниченные размеры, padding, opacity, rotation и breakpoint overrides;
- видимость декора на desktop/mobile/export.

Все координаты и размеры хранятся в нормализованном виде либо как токены заранее определённой шкалы. Свободные CSS-единицы и селекторы не допускаются.

## 10. Поверхности экспорта

| Формат | Размер v1 | Фото | Фразы | Особенность |
|---|---:|---:|---:|---|
| Story | `1080 × 1920` | Ровно 3 либо блок скрыт | 2 | Вертикальная адаптация коллажа |
| Post | `1080 × 1350` | 2 из выбранной тройки либо блок скрыт | 3 | Плотная адаптация коллажа |
| A4/Print preview | `1240 × 1754` | Ровно 3 либо блок скрыт | 3 | Коллаж по эталону, около 150 DPI |

Web и export используют одни семантические данные, asset refs, рамки, crop и safe areas, но разные технические компоненты. Preview и скачиваемый файл обязаны строиться одним export-renderer.

## 11. `TemplateProfile`

Типы, runtime-валидация и `defineTemplate` реализованы в `src/lib/templates/profile.ts`. Реестр и диспетчер находятся в `src/lib/templates/registry.ts` и `src/lib/templates/dispatcher.ts`. Ниже сохранена сокращённая форма профиля для чтения документа; фактический тип дополнительно фиксирует нормализованные фоторамки, разрешённые декоративные слои, локальные asset refs и бюджеты в байтах.

Asset-контракт художественной кожи не допускает одной произвольной картинки, растянутой на все поверхности:

- `hero`, `summary`, `qualities`, `messages`, `memories`, `quotes` и `closing` получают семь независимых подложек, каждая с общим preset-ом растяжения, собственной safe-area и, при необходимости, отдельным `mobileAsset`;
- `greetingCards` содержит либо пустой список, либо ровно четыре подложки; renderer назначает их карточкам по кругу `1 → 2 → 3 → 4 → 1` и сохраняет тот же порядок в диалоге всех поздравлений;
- portrait- и landscape-фоторамки состоят из `base`, нормализованного `aperture`, фотографии с сохранённым crop, `overlay` и `captionArea`; новые оформления выбирают общий preset и не двигают окно рамки попиксельно;
- индивидуальные координаты текста для отдельных художественных файлов запрещены: текст остаётся в семантической safe-area и адаптируется только в пределах утверждённых content-пресетов.

```ts
defineTemplate({
  id: "new-template",
  family: "universal-v1",
  layoutPreset: "route-v1",
  metadata: { name, description, accent, preview },
  assets: {
    page,
    hero,
    summary,
    qualities,
    greetingCards,
    messagePhotoFrames,
    memories,
    memoryPhotoFrames,
    quotes,
    closing,
    decor
  },
  typography: { heading, body, handwritten },
  colors: { page, text, muted, accent, surfaces },
  intro: { surface, text, accent, mark, pattern },
  motion: { preset: "calm" | "playful", revealSections, photoViewer },
  public: { blocks: ["hero", "qualities", "memories", "quotes"] },
  export: { profile: "universal-export-v1" },
  performance: { networkBudget, decodedMemoryBudget },
  demo: { fixture: "full-card-default" }
});
```

### Motion-контракт

`motion` — необязательная декларативная часть `TemplateProfile`. Она не содержит CSS, callbacks или произвольные сценарии и поддерживает только:

- `preset: "calm" | "playful"` — интенсивность общей motion-системы;
- `revealSections` — одноразовое появление секций и последовательное появление карточек через общий `IntersectionObserver`;
- `photoViewer` — интерактивное окно фотографии и полноэкранный просмотр.

Общий web-renderer применяет одинаковую семантику движения ко всем профилям: desktop использует lift, тень и ограниченный zoom фотографии; mobile — одноразовый reveal и tactile press только для реально интерактивных элементов. `playful` допускает немного большую амплитуду и детерминированный микроповорот карточек, `calm` исключает подпрыгивание и остаётся визуально сдержанным. Постоянно движущийся декор запрещён.

Фотопросмотр открывается только при `photoViewer: true`, сохраняет подпись, поддерживает `Escape`, стрелки клавиатуры, экранные кнопки и свайп, блокирует прокрутку фона и возвращает фокус на исходную фотографию. Публичная поверхность строит галерею только из разрешённых публичных фотографий.

`prefers-reduced-motion: reduce` оставляет контент сразу видимым и отключает reveal, lift, zoom, поворот и tactile scale. Export-renderer остаётся полностью статичным: motion-профиль влияет только на web-просмотр и не меняет Story/Post/A4.

## 12. Завершение этапа 1

Контракт утверждён пользователем 11 августа 2026 года с уточнениями по дате события, полному выводу подписей и главного поздравления, потоку лучших фраз и единственной трёхфотографической схеме «Моментов».

Критерий перехода к этапу 2 выполнен. Следующая реализация начинается с типов и валидации `TemplateProfile`, канонической модели Hero с `eventDate`, публичного флага `showEventDate` и тестовых fixture на граничные значения 45/100/500 символов и ровно три фото.
