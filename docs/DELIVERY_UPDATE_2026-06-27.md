# Delivery update — 2026-06-27

## Статус

Продолжается локальная доводка `/manage/[manageToken]` по плану `docs/plan.md`. Все запланированные пункты реализованы, пройден скриншотный QA и локальный `npm run build`.

## Что сделано

1. Реализованы все 15 пунктов `docs/plan.md`:
   - header: `Копировать ссылку` в одну строку;
   - карточки вариантов `Поздравления` выше и читаемее;
   - формулировка готовности фото `Для блока нужно X фото · доступно Y`;
   - фото-карточки упрощены;
   - dropdown `Использовать в блоке` без слова `занято`;
   - drag icon мягче;
   - аватары без коричневого;
   - статусы ярче;
   - AI-кнопка `✨ Сократить текст` как secondary;
   - tabs без двойного подчёркивания;
   - убрана кнопка `…` рядом с `Добавить вручную`;
   - подсказки тише;
   - preview asset привязан к `Бумажный классический`;
   - пояснения в блоках короче.

2. Дополнительно по результатам QA:
   - кастомный dropdown `Использовать в блоке` вместо нативного `<select>`;
   - trigger показывает только позицию (`Поздравления (1)`), в списке — `Позиция — поменять местами`;
   - исправлен swap фото между занятыми слотами;
   - сообщения при сохранении фото стали контекстными;
   - форма загрузки фото: `Подпись` (широкое) | `Использовать в блоке` (узкое), ниже `Выберите файл` → статус → `Добавить фото`;
   - кнопка `Загрузить фото` остаётся оранжевой и на месте, форма открывается после фильтров;
   - кнопка `Добавить вручную` работает как toggle (`Скрыть форму`);
   - блок `Поздравления` в `Составе открытки` свёрнут по умолчанию.

3. Локальная проверка:
   - `npm run build` проходит;
   - `npm run lint` падает только на прежних ошибках `setState in useEffect` в не тронутых файлах.

## Что осталось

1. ~~Закоммитить изменения~~ — сделано: `0d48231 Polish /manage UI per plan.md: photo cards, custom slot dropdown, upload form, docs updates`.
2. ~~Сделать push~~ — сделано: `main` на `origin` обновлён.
3. Обновить VPS и проверить production health check.
4. Пройти mobile QA на реальном телефоне:
   - загрузка фото;
   - подпись;
   - выбор места;
   - swap;
   - финальная открытка `/gift/[finalSlug]`.
5. Приоритетно починить оставшиеся lint-ошибки `setState in useEffect`.
6. Обновить или удалить упавший тест `src/lib/final-card/view-model.test.ts`.
