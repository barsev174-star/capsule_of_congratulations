# Текущий production-статус и ближайшие задачи

Дата актуализации: 2026-08-26.

Этот документ — текущий источник правды для Slovesto. Исторические планы и чек-листы сохраняют контекст, но их незакрытые галочки не считаются актуальным backlog без повторной проверки по этому документу и текущему коду.

## Production сейчас

- Основной домен: `https://slovesto.ru`; `www.slovesto.ru` перенаправляет на основной домен с сохранением пути и query-параметров.
- Приложение работает на VPS в `/home/deploy/capsule` через Docker Compose, PostgreSQL и Caddy.
- Актуальная схема приложения включает миграции `0001`–`0032`; универсальная публичная открытка использует payload версии 2.
- Оплата через Robokassa включена, протестирована и принимается на production. Сервер проверяет подпись ResultURL, а доступ к финальной открытке выдаётся только после подтверждения оплаты либо явного административного grant.
- Стоимость финальной открытки — 399 ₽ единоразово. Подписки, доплаты за отдельный шаблон и несколько тарифов не используются.
- Доступны шесть продуктовых шаблонов: `Бумажный классический` (`paper-birthday`), `Маршрут` (`route-adventure`), `Вместе` (`team-editorial`), `Школьный классический` (`school-classic`), `Школьный коллаж` (`school-scrapbook`) и `Детство в рисунках` (`kindergarten-doodles`). Все шесть можно выбрать при создании открытки; для всех действует единый платный контракт.
- `northern-light` и `daylight-proof` остаются внутренними studio-профилями и не входят в продуктовый каталог.
- Полный production-сценарий неоднократно пройден владельцем проекта; критических замечаний по созданию, сбору материалов, оплате, передаче и просмотру открытки нет.
- Контрольное напоминание о событии фактически получено 10.08.2026; повторная разработка reminder-flow не требуется.
- Ночная резервная копия PostgreSQL и uploads настроена с хранением 14 дней. Предрелизная production-пара `20260826-140113` успешно прошла checksum и контрольное восстановление в изолированные временные PostgreSQL-базу и каталог без изменения рабочих данных (32 миграции). Фактический каталог backup — `/home/deploy/capsule/backups`; процедура описана в `docs/VPS_DEPLOYMENT_NOTES.md`.
- Критические серверные ошибки после записи в журнал ставятся в PostgreSQL-очередь внешних оповещений. Email включается через Resend, Telegram — при наличии bot token и chat id; одинаковые события дедуплицируются, неуспешная доставка повторяется до пяти раз.
- Публичные ссылки имеют статические версионированные Open Graph/Twitter Card 1200×630 для всех продуктовых шаблонов. Изображение показывает только безопасную общую композицию и художественное превью шаблона: имя получателя, повод, поздравления, фотографии и секретные ссылки в него не попадают. Готовый PNG отдаётся без серверного рендера, с `Content-Length` и долгим immutable-cache для совместимости с Telegram и другими мессенджерами.
- Релиз 25.08.2026 добавил продуктовые шаблоны `kindergarten-doodles` и `team-editorial`, их демонстрационные открытки, локальные шрифты, безопасные OG-изображения и все скачиваемые форматы. На главной странице и `/example` каталог использует единый порядок `3 + 3`: сверху «Бумажный классический», «Маршрут», «Вместе»; снизу «Школьный классический», «Школьный коллаж», «Детство в рисунках».
- Текущий код web — `e0b9c22`, опубликован 26.08.2026. Доступны четыре SEO-страницы: учителю, воспитателю, коллеге и ко дню рождения. Последняя использует «Бумажный классический», отдельный пример друзей и семьи, шесть новых фото Кристины, SEO-разметку, first-touch и анимации. Состав и проверки: `BIRTHDAY_SEO_LANDING_2026-08-26.md`.
- Опубликована `/admin/analytics`: когорта открыток за 7/30 дней, подтверждённые оплаты, передача/открытие, четыре SEO-страницы, источники и статистика участников; SQL-сводка больше не ограничена последними 10 000 событиями. Точный SQL проверен на production за оба периода без ошибок в read-only транзакции. Ограничения: `docs/ACQUISITION_ANALYTICS_2026-08-26.md`.
- Приглашения `/join/{slug}` используют безопасное OG-превью выбранного шаблона; наличие картинки в head и noindex проверено на production с User-Agent TelegramBot. Фактическое отображение в пользовательском мессенджере проверяется отдельно: `docs/PUBLIC_SHARE_EXPORTS.md`.

## Зафиксированный продуктовый контракт

1. Создание, настройка, приглашение участников, сбор и модерация до 100 поздравлений, preview и ограниченный AI доступны до оплаты.
2. Оплата 399 ₽ открывает финальную подготовку, передачу открытки получателю, финальную ссылку, расширенный AI-лимит и доступ минимум на 12 месяцев.
3. В стоимость входят все шесть доступных продуктовых шаблонов. Выбор шаблона не меняет цену.
4. После подтверждённой оплаты повторная публикация той же открытки не требует новой оплаты и не меняет финальную ссылку.
5. Черновик хранится 90 дней после последней активности. Опубликованная открытка хранится минимум 12 месяцев.
6. При удалении открытка сразу скрывается, 30 дней доступна для восстановления, затем окончательно удаляется вместе со связанными файлами.
7. Запросы на возврат принимаются через `support@slovesto.ru`; сроки ответа и порядок возврата опубликованы на странице `/refunds`.

## Что реализовано и не является backlog

- Платёжный checkout Robokassa, ResultURL, защита от повторного подтверждения, чек, возврат и платный доступ.
- Шесть продуктовых шаблонов и единая цена для них.
- Production-проверка пользовательского сценария без критических замечаний.
- Пакеты замечаний №1–3, включая лимит главного поздравления 700 символов, кадрирование фотографий, заставку и производительность загрузки фото.
- Текущая функциональность управления фотографиями. Дополнительная переработка хранения и модели фотографий сейчас не запланирована.
- Напоминания, обращения в поддержку, админка, аналитика и журнал критических ошибок.
- Контрольное восстановление production-backup, внешняя доставка критических alerts и безопасные Open Graph/Twitter Card публичных ссылок.
- Четыре SEO-страницы (учитель, воспитатель, коллега, день рождения), их sitemap/canonical/FAQPage/BreadcrumbList, выбор соответствующего шаблона при создании и события `seo_landing_view`, `seo_example_click`, `seo_create_click`.

## Оставшиеся задачи

### Приёмка опубликованного релиза — 26.08.2026

Выпуск `e0b9c22` завершён: web/PostgreSQL healthy, HTTP/SEO/ассеты, SQL аналитики 7/30 и серверное превью приглашения проверены. Остались ручная сверка нескольких известных оплат и бесплатных выдач доступа в аналитике и повторная отправка ссылки в пользовательском мессенджере. Название мессенджера пока не уточнено; его кеш и отображение серверными проверками не подтверждены.

Подготовленные `quote-card-*-export` PNG/WebP воспитателя сохранены в репозитории без переключения действующего профиля. PDF из `output/` остаётся локальным артефактом. Механизм запросов участника остаётся только в backlog и в этот выпуск функционально не входит.

### Эксплуатация и юридическая фиксация

1. Зафиксировать результат проверки опубликованных юридических текстов профильным специалистом, если такая проверка ещё не оформлена документально.

### Развитие после стабилизации

1. Оценить SEO-страницы как канал привлечения.
2. Проверить гипотезу Pinterest.
3. Новые художественные шаблоны выпускать только после визуальной приёмки в ателье и полного `npm run template:launch-check`.
4. Запросы участника на корректировку собственного поздравления и передача поздравления за другого человека — только с принятием организатором, без прямого изменения открытки. Хотелка уточнена 26.08.2026, не реализована: `docs/PARTICIPANT_REQUESTS_BACKLOG_2026-08-26.md`.

## Правило актуализации

После изменения оплаты, состава продуктовых шаблонов или production-инфраструктуры сначала обновляется этот документ. `docs/plan.md`, ранние pre-payment чек-листы и планы отдельных пакетов не должны использоваться как список незавершённых задач без сверки с текущим статусом.
