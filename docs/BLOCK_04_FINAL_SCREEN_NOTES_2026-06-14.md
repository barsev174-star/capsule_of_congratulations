# Блок 4: финальный экран и управление композицией

Актуальный статус проекта после первичного дизайна и MVP-flow см. в `docs/PROJECT_HANDOFF_2026-06-23.md`. Этот файл остается исторической заметкой по блоку финального экрана.

Дата: `2026-06-14`

## Что сделали

1. Добавили настройку формата блока поздравлений в админке:
   - `Две колонки`
   - `Один ряд с прокруткой`
   - `Два ряда с прокруткой`
2. Добавили сохранение этих настроек в данные открытки.
3. Добавили порядок поздравлений в данные и кнопки `Выше / Ниже` для организатора.
4. Сделали отдельный экран `/gift/[finalSlug]/messages`, где удобно читать только поздравления.
5. Добавили в админке живую схему структуры открытки, которая меняется сразу при переключении настроек.
6. Оставили текущую block-based архитектуру и усилили ее вместо добавления новых типов открыток.
7. Сохранили обязательные проверки качества:
   - логирование изменений организатора
   - тесты
   - успешная сборка

## Что это дает продукту

1. Организатор теперь управляет не только наличием блоков, но и ритмом чтения открытки.
2. Финальный экран становится гибче без усложнения MVP новыми сущностями.
3. Для длинных наборов поздравлений появился более удобный сценарий просмотра.

## Что сознательно отложили

1. Суперблок для длинных поздравлений.
2. Отдельные лимиты длины для обычного блока и суперблока.
3. Выбор медиаблоков вместо суперблока по сложным правилам.
4. Более глубокий визуальный конструктор с drag-and-drop.

## Следующие разумные шаги

1. Посмотреть руками, какой режим поздравлений ощущается сильнее на реальных данных.
2. Вернуться к идее суперблока только после живого теста обычных режимов.
3. Потом перейти к архитектурному слою и подготовке к следующему этапу MVP.

## Обновление этого блока

1. Страница управления переработана в более цельный `layout studio`, а не в набор разрозненных карточек справа.
2. Схема финального экрана теперь показывает блоки сверху вниз, ближе к реальной структуре открытки.
3. Выбор формата поздравлений сделан компактнее и логичнее сгруппирован.
4. Добавлен новый режим `одна колонка + медиа`.
5. Для режима `одна колонка + медиа` добавлен выбор:
   - одно вертикальное место под фото
   - два горизонтальных места под фото
6. Слабый текстовый предпросмотр админки убран как малоинформативный.

## Обновление 2026-06-15

1. `Состав открытки` окончательно стал единой точкой управления структурой финального экрана, без отдельного дублирующего блока рядом.
2. `Обложка` и `Финал` зафиксированы по краям композиции:
   - их нельзя удалить;
   - их нельзя перетаскивать;
   - внутри блока это явно подписано.
3. Перетаскивание остальных блоков усилено визуально:
   - карточка под курсором уходит в drag-состояние;
   - место вставки подсвечивается отдельной линией;
   - организатор видит, куда блок встанет до отпускания.

### Что это дает продукту

1. Компоновка стала ближе к реальной ментальной модели: есть один список блоков, который и есть открытка.
2. Убирается риск случайно сломать начало или конец сценария вручения.
3. Drag-and-drop ощущается понятнее и дороже даже без тяжелого отдельного конструктора.
4. Экран `Оформление открытки` получил отдельную правую колонку с:
   - карточкой текущего шаблона;
   - живым компактным предпросмотром;
   - быстрым срезом по текущему состоянию открытки.
5. Выбор шаблона окончательно поднят наверх, а отдельный третий шаг убран.
6. `Состав открытки` переработан ближе к референсу:
   - список блоков стал похож на настоящий конструктор;
   - вместо крестиков используются переключатели;
   - блоки можно раскрывать для дополнительных настроек и пояснений.
7. Экран `Оформление открытки` дополнительно подтянут под целевой макет:
   - шаги сверху стали компактным stepper;
   - карточка текущего шаблона облегчена и перенесена в шапку;
   - блок `Поздравления` получил схемы layout-опций и более точные контролы.
8. Вкладка `Поздравления и фото` переведена в тот же визуальный язык:
   - сверху добавлена короткая сводка по длине текстов и количеству фото;
   - список поздравлений стал похож на редакторские карточки с раскрытием и мягкими тумблерами;
   - правый rail больше не выглядит как техническая админка и теперь ближе к референсу с preview, фото и подсказками.
9. Перестановка поздравлений внутри вкладки теперь тоже соответствует новому уровню интерфейса:
   - вместо кнопок `выше / ниже` используется drag-and-drop;
   - карточка следует за курсором через drag-state;
   - место вставки показывается заранее отдельной теплой линией.
 
## Update 2026-06-15: manage editor polish

1. `Оформление открытки` and `Поздравления и фото` are now treated as two parts of one organizer editor:
   - design/composition lives in the first tab;
   - moderation/content/media lives in the second tab.
2. Composition drag-and-drop was refined:
   - fixed anchor blocks stay locked;
   - movable blocks are dragged from the handle;
   - the drag preview no longer feels attached to the center of the row.
3. Contribution moderation now has explicit visibility grouping:
   - active contributions are the main working group;
   - hidden contributions are retained but pushed below active cards;
   - returning a hidden contribution to active places it at the end of the active group.
4. Status changes collapse the contribution card before movement, so moderation does not move a large expanded editor around the page.
5. The next visual target is to make the right-side previews more faithful to the real final card and less like admin helper cards.
