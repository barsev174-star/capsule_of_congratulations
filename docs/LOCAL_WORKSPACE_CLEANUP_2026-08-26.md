# Проверка локальной папки и безопасная очистка

Дата замера: 26.08.2026. Ничего не удалено.

## Можно удалить без потери исходников

| Каталог | Размер | Что это |
| --- | ---: | --- |
| `.next/` | 1 997,75 MiB | результат локальных сборок Next.js |
| `.npm-cache/` | 1 442,30 MiB | локальный кеш npm |
| `tmp/` | 487,25 MiB | временные рендеры, PDF и логи проверок |
| `screenshots/` | 168,36 MiB | воспроизводимые QA-снимки |

Всего без переустановки зависимостей: **4 095,67 MiB, примерно 4,00 GiB**.

Дополнительно можно удалить `node_modules/` размером **466,29 MiB**, но перед следующей разработкой понадобится `npm install`. Полная экономия тогда составит **4 561,95 MiB, примерно 4,46 GiB**.

Удалять следует только перечисленные абсолютные каталоги после отдельного подтверждения владельца. Общий `git clean -X` использовать нельзя.

## Не удалять автоматически

- `data/` — 26,12 MiB локальных прикладных данных;
- `public/uploads/` — 164,06 MiB пользовательских загрузок;
- `output/` — ручной PDF `Ирина-Олеговна-print-updated.pdf`, 0,43 MiB;
- `.env.local`, `key.txt`, логи с возможным техническим контекстом;
- игнорируемые PNG в `docs/` — среди них исходные референсы и результаты визуальной приёмки;
- `template-assets/`, `public/`, `assets/` — исходники и продуктовые ассеты;
- `.git/` — история репозитория. Из 742,7 MiB около 713,6 MiB находятся в pack-файлах; удалять каталог нельзя. Обычное обслуживание даст лишь небольшую экономию относительно риска и сейчас не требуется.

## Неиспользуемые карточки воспитателя

Файлы `quote-card-{1,2,3}-export` — три широкие бумажные подложки для цитат с воздушным змеем, корабликом и кубиками. Это не новая пользовательская функция. Они подготовлены 24.08.2026 как вариант экспортного оформления «Детства в рисунках», но профиль их не использует.

Исходные PNG занимают около 125 KiB, опубликованные WebP — около 38 KiB. Экономия несущественна. Поскольку владелец не помнит согласования этого варианта, в следующем пакете следует удалить шесть неиспользуемых файлов и исправить упоминания в документации. Сначала проверить отсутствие ссылок через `rg` и `npm run template:check`.
