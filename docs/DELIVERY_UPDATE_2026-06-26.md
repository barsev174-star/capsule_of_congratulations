# Delivery update — 2026-06-26

## Статус

Production MVP работает на `darislova.ru`. Сейчас активная работа идет не на серверной инфраструктуре, а на качестве продукта: страница участника уже выложена, экран менеджера перерабатывается локально.

## Поставлено на production

1. Домен `darislova.ru` и `www.darislova.ru` открываются по HTTPS.
2. VPS stack `capsule` поднят и проверен.
3. Backups базы и uploads настроены.
4. Страница участника `/join/[slug]` отполирована и выложена.
5. На VPS актуальный коммит после polish участника:

```text
c558b6e Polish join contribution page
```

## В работе локально

1. Переработка `/manage/[manageToken]`.
2. Улучшение mobile usability менеджера.
3. Новая медиабиблиотека:
   - загрузка фото по кнопке;
   - крупные превью;
   - подпись;
   - выбор места в открытке;
   - swap фото между занятыми слотами.
4. Проверка, что подпись фото корректно попадает в финальную открытку.

## Локальная проверка manager — 2026-06-26

Перед коммитом локально пройден безопасный browser smoke-test вкладки `Поздравления и фото`:

1. URL проверки: `http://localhost:3000/manage/5bc64fce98ef97e6d1432d8e03aceb82?tab=content`.
2. Пояснения левой и правой колонок выровнены по одной линии.
3. Фильтры левой и правой колонок выровнены по одной линии.
4. Фильтры медиатеки `Все`, `Горизонтальные`, `Вертикальные` переключаются.
5. Форма ручного добавления открывается после фильтров и не сдвигает их.
6. Форма загрузки фото открывается по кнопке `Загрузить фото`.
7. `npm.cmd run build` проходит.

Текущая локальная стадия: manager rework закоммичен локально.

```text
928b801 Rework manager content studio
```

После коммита пройдены короткие локальные проверки финальной открытки и mobile viewport менеджера. Перед production deploy осталось сделать push, обновить VPS и проверить домен на телефоне.

## Следующая поставка

Перед следующим deploy нужно:

1. сделать push текущего manager rework;
2. обновить VPS;
3. проверить `https://darislova.ru`;
4. проверить тестовый manager/gift сценарий на телефоне;
5. после деплоя отдельно пройти ручной mobile QA загрузки фото, подписи, выбора места и сохранения.
